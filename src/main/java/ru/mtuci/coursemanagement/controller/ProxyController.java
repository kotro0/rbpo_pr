package ru.mtuci.coursemanagement.controller;

import org.springframework.web.bind.annotation.RestController;

/**
 * Proxy endpoint removed due to SSRF vulnerability.
 * The /api/proxy endpoint allowed unrestricted access to any URL,
 * which could be exploited to access internal services.
 */
@RestController
public class ProxyController {
    // Proxy functionality removed for security reasons
}
